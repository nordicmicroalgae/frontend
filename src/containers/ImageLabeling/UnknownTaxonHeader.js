import React from 'react';

const UnknownTaxonHeader = () => (
  <header className="unknown-taxon-header">
    <h1>Unknown taxon</h1>
    <div className="unknown-taxon-subtitle">
      These images have not been assigned to a taxon yet.
    </div>
  </header>
);

export default UnknownTaxonHeader;
