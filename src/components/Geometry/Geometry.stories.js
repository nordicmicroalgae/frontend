import React from 'react';
import { ChainOfSpheres, Cylinder, Ellipsoid, Sphere, Cone, ConePlusHalfSphere, Trapezoid, TwoCones, Parallelepiped } from './';

export default { title: 'Geometry' };

export const chainOfSpheres = () => (
  <ChainOfSpheres />
);

export const cylinder = () => (
  <Cylinder />
);

export const ellipsoid = () => (
  <Ellipsoid />
);

export const sphere = () => (
  <Sphere />
);

export const cone = () => (
  <Cone />
);

export const conePlusHalfSphere = () => (
  <ConePlusHalfSphere />
);

export const trapezoid = () => (
  <Trapezoid />
);

export const twoCones = () => (
  <TwoCones />
);

export const parallelepiped = () => (
  <Parallelepiped />
);
